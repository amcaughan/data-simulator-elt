name: sample_api_polling_01
version: "1.0.0"
config-version: 2

profile: sample_api_polling_01

model-paths:
  - models

macro-paths:
  - macros

clean-targets:
  - target
  - dbt_packages

on-run-start:
  - "{{ ensure_raw_external_table() }}"

models:
  sample_api_polling_01:
    +materialized: table
    +format: parquet
    +table_type: hive
