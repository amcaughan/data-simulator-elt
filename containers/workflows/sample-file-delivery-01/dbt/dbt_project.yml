name: sample_file_delivery_01
version: "1.0.0"
config-version: 2

profile: sample_file_delivery_01

model-paths:
  - models

macro-paths:
  - macros

clean-targets:
  - target
  - dbt_packages

on-run-start:
  - "{{ ensure_raw_external_table() }}"

models:
  sample_file_delivery_01:
    +materialized: table
    +format: parquet
    +table_type: hive
